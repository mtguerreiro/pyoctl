from setuptools import setup

setup(name='pyoctl',
      version='0.1.0',
      description='Optimal control with Python',
      #url='https://github.com/,
      #author='Me',
      author_email='marcotulio.guerreiro@gmail.com',
      packages=['pyoctl'],
      #install_requires=['numpy>=1.14.3',],
      )
